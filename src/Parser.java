import java.util.List;
import java.util.Scanner;

public class Parser {
    // Take in an array of TOKENS, and create a tree structure
    public void parseTokens(String source) {
    }
    // Report syntax errors
}
